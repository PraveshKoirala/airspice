{
  "name": "mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AirSpice MCP server (issue #40): a local stdio Model Context Protocol server that exposes AirSpice's deterministic engine (air-ts validate/normalize/patch/registry/render + sim-wasm ngspice) to any MCP client. Thin transport + wiring over the SAME tool contracts the in-browser agent (#18) uses; zero forked engine logic.",
  "main": "./dist/index.js",
  "bin": {
    "airspice-mcp": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -b",
    "start": "node ./dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agent": "*",
    "air-ts": "*",
    "sim-wasm": "*"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "~5.6.3",
    "vitest": "^2.1.8"
  }
}
