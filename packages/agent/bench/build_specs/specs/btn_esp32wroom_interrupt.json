{
  "id": "btn_esp32wroom_interrupt",
  "title": "Wake-on-press button (ESP32-WROOM)",
  "category": "button",
  "prompt": "I want a single wake/action button on my ESP32-WROOM-32 wired to a GPIO so firmware can attach an interrupt to it. Give it a pull-up to 3.3 V so it idles high and pulls low on press. For the bench check the button is open and the input idles near the rail.",
  "mcu": "esp32_wroom_32",
  "fidelity": "faithful",
  "criteria": {
    "required_components": [
      { "type": "mcu", "count": 1, "part": "ESP32-WROOM-32" },
      { "type": "resistor", "count": 1 }
    ],
    "connectivity": [
      "pullup(net=<btn>, to=<v3v3>)",
      "gpio_input(net=<btn>, mcu=ESP32-WROOM-32)"
    ],
    "firmware_intent": [ "read_gpio(net=<btn>)" ],
    "erc_clean": true,
    "sim_assertion": { "net": "btn", "min_v": 3.2, "max_v": 3.4 }
  },
  "turn_budget": 4
}
